<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       https://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/mvc
       https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!--
        开启扫描器 交给springmvc处理
    -->
    <!--controller类 接受请求的servlet-->
    <context:component-scan base-package="com.node.controller"/>
    <!--创建的aop切片类-->
    <context:component-scan base-package="com.node.aspects"/>
    <!--全局异常-->
    <context:component-scan base-package="com.node.exceptions"/>
    <!--开启异步处理-->
    <context:component-scan base-package="com.node.async"/>

    <!--
        启用JSON注解功能
    -->
    <mvc:annotation-driven />

    <!--
        文件上传解析类
    -->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
        <property name="defaultEncoding" value="UTF-8"/>
        <property name="maxUploadSizePerFile" value="10485660" />
    </bean>


    <!--全局扫描器-->
    <mvc:interceptors>
        <mvc:interceptor>
            <!--表示所有的都会扫描-->
            <mvc:mapping path="/**"/>
            <!--登录与获取与验证码的功能是可以不被监视的-->
            <mvc:exclude-mapping path="/login"/>
            <mvc:exclude-mapping path="/captcha/getCaptcha"/>
            <!--指定扫描器的类-->
            <bean class="com.node.interceptors.LoginInterceptors"/>
        </mvc:interceptor>
    </mvc:interceptors>




</beans>